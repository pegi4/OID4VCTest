import { Result } from './result.js';
import './detailedError.js';

interface PrivateKeyToDidArgs {
    privateKey: string;
    didMethod: string;
}
interface PrivateKeyToDidResponse {
    did: string;
}
declare const privateKeyToDid: (args: PrivateKeyToDidArgs) => Promise<Result<PrivateKeyToDidResponse>>;

export { PrivateKeyToDidArgs, PrivateKeyToDidResponse, privateKeyToDid };
